# Optional: required for fresh AI word sets and AI-generated images.
# Keep this server-side. Do not rename it to VITE_OPENAI_API_KEY.
OPENAI_API_KEY=

# Optional text model override. The default is gpt-5-mini.
OPENAI_TEXT_MODEL=gpt-5-mini
